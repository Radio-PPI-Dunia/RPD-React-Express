import React, { Component } from 'react';

export default class Team extends Component {

    // TODO: Build a grid using Materializecss => Google it!

    render(){
        return(
            <div>
                Team page
            </div>
        )
    }
}